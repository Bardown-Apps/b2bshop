/**
 * Fixed footer for Team Order Form dialog.
 * Placeholder for future actions (e.g. Submit, Cancel).
 */
export function TeamOrderFormFooter() {
  return (
    <div className="sticky bottom-0 z-10 shrink-0 border-t border-gray-200 bg-white px-4 py-3 sm:px-6">
      <div className="flex justify-end gap-2">
        {/* Reserve space for future: Submit / Cancel etc. */}
      </div>
    </div>
  );
}
